git init
git add .
git remote.......
git commit "my-first-commit"
git push -u  origin master

rm .git    - to remove already initialized git repository

if this is the error $ git push -u origin master
remote: Repository not found.
fatal: repository 'https://github.com/kiotie32/arbit-text.git/' not found

control panel - credential manager - windows credential - remove all git related credentials and then git push
