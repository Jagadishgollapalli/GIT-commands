git init
git add .
git commit "my-first-commit" 
git remote.......
git push -u  origin master 

/////do not use git push -u  origin main 👍🙌😁✌️///////
(if error is error: src refspec main does not match any          
error: failed to push some refs to 'https://github.com/Jagadishgollapalli/react-hooks-functionality-.git')

rm .git    - to remove already reinitialized git repository instead of rm -rf .git  //do not use this

if this is the error $ git push -u origin master
remote: Repository not found.
fatal: repository 'https://github.com/kiotie32/arbit-text.git/' not found

control panel - userAccounts- credential manager - windows credential - remove all git related credentials and then git push


when starting nodemon gave trouble:
step 1: -Press the windows-button on your keyboard.
step 2: -Type ‘PowerShell’
step 3: -Right-click Windows PowerShell
step 4: -Click Run as Administrator
step 5: -Run the following command and confirm with ‘Y’
Try this. 

Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope LocalMachine





//sample use efect for axios api call
 useEffect(() => {
    console.log('effect');
    axios.get(`https://restcountries.eu/rest/v2/all`).then((response) => {
      console.log(response);
      setCountries(response.data);
    });
  }, []);
